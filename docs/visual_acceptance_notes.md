# Yesok 2.0 第二阶段浏览器肉眼验收记录

## C 端首页

浏览器已打开 H5 预览根路径，Uni-app 正常挂载，页面不再白屏。截图显示热带奢华首页已渲染，顶部使用 `/static/img.png` Banner，底部显示原生 Tabbar：首页、服务、我的。页面文本中已出现从 `/api/v1/services` 动态返回的服务数据，包括“越南机场接送”“越南签证加急”“商务包车”“商务翻译”“企业落地咨询”。热门服务卡片中显示价格和“去咨询”按钮。

## 待继续验收

下一步继续查看控制台、打开 B 端后台，并执行 C 端下单、B 端状态推进、财务流水更新的完整闭环自测。

## B 端后台

浏览器已打开 `#/pages/admin/index`，页面不再白屏。登录页正常渲染并预填 `admin / 123456`，点击“进入后台”后成功进入 Yesok 2.0 管家后台。后台左侧菜单包括数据看板、订单中心、服务配置、财务流水、用户矩阵；数据看板正常显示总订单、待受理、已完成、确认收入、服务上架 5 项和客户矩阵 1 位等真实统计信息。

## B 端订单与财务闭环肉眼确认

点击“订单中心”后首次进入仍显示缓存统计；点击“刷新数据”后后台通过真实接口重新拉取到第二阶段验收数据。页面显示总订单 2、待受理 1、确认收入 65000000，并在订单中心展示 `第二阶段验收客户` 对应的豪华接机订单：订单号 `YS202605151848282770`，状态为已收款，支付状态为 `paid`，`form_data` 中包含 `arrival_city`、`flight_no`、`passenger_count`、`remark`、`service_code`、`service_name`、`submitted_at` 等字段，证明 C 端下单与 B 端订单读取链路已跑通。

## C 端白屏修复后复验

重新启动 H5 服务后，直接访问 `/#/pages/home/index` 曾因浏览器保留错误页出现公网不可用提示；切换到 H5 根路径 `/#/` 后 Uni-app 路由正常初始化，页面肉眼可见热带奢华首页。页面顶部渲染 `/static/img.png` Banner，包含 `Yesok Vietnam`、高透搜索胶囊、动态服务分类和热门服务卡片，底部原生 Tabbar 显示“首页、服务、我的”。接口返回的服务数据已显示为“越南机场接送、越南签证加急、商务包车、商务翻译、企业落地咨询”等，确认不是白屏且不是硬编码静态页面。

## B 端财务流水复验

进入 `#/pages/admin/index` 后，后台已正常渲染侧边导航、数据看板、刷新按钮和业务统计，不再白屏。切换至“财务流水”面板后，页面展示 `YS-PAY-1778870908860378957 · 订单 #2`、金额 `650000 VND` 与状态 `success`，确认财务流水字段映射已修复，未再出现 `NaN` 金额。
